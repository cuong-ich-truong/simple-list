import styled from 'styled-components';
import Delete from '@material-ui/icons/Delete';

const StDeleteIcon = styled(Delete)`
  cursor: pointer;
  :hover {
    color: lightcoral;
  }
`;

export default StDeleteIcon;
